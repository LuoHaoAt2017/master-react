复杂的问题被分解成三个⼩问题
- 如何产⽣事件，这是发布者的责任，在RxJS中是Observable对象的⼯作。
- 如何响应事件，这是观察者的责任，在RxJS中由subscribe的参数来决定。
- 什么样的发布者关联什么样的观察者，在RxJS中也就是何时调⽤subscribe。

在RxJS中，作为迭代器的使⽤者，并不需要主动去从Observable中“拉”数据，
⽽是只要subscribe上Observable对象之后，⾃然就能够收到消息的推送，
这就是观察者模式和迭代器两种模式结合的强⼤之处。